<%# pass variables to client javascript (original slideshow) -%>
<% content_for :slideshow do -%>

	window.json_url = "<%= @json_url.html_safe %>";
	window.cache = <%= @cache %>;
	window.preview = <%= @preview %>;
	window.data_update_interval = <%= @data_update_interval %>;
	window.ctrl_update_interval = <%= @ctrl_update_interval %>;

<% end %>
