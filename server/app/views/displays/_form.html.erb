<%= simple_form_for(@display, :url => url) do |f| %>
  <div class="inputs">
    <%= f.input :hostname %>
    <%= f.input :active %>
    <%#= f.association :channels, :collection => Channel.find_all_by_school_id(@school.puavo_id) %>
    <%= render :partial => 'display_channels' %>
  </div>

  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
